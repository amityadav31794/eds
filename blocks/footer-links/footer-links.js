export default async function decorate(block) {
  // The footer-links block has 3 rows:
  // Row 1: primary links (cell 1) + social icons (cell 2)
  // Row 2: utility/legal links (cell 1)
  // Row 3: copyright info (cell 1)
  // No major DOM restructuring needed — CSS handles the layout.
}
